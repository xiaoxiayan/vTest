#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/ppp/Documents/my/vTest/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/ppp/Documents/my/vTest/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../10.107.103.115+8001+vite-node@0.32.0_@types+node@20.3.2/node_modules/vite-node/vite-node.mjs" "$@"
else
  exec node  "$basedir/../../../../../10.107.103.115+8001+vite-node@0.32.0_@types+node@20.3.2/node_modules/vite-node/vite-node.mjs" "$@"
fi
